services:
  hugo:
    image: hugomods/hugo:exts
    working_dir: /src/site
    command: server --bind 0.0.0.0 --baseURL http://localhost:1313/ --appendPort=false --buildDrafts --buildFuture --disableFastRender
    ports:
      - "1313:1313"
    volumes:
      - .:/src
